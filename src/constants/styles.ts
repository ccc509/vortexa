import {style} from "typestyle";

export const clearButton = style({
    color: 'black', 
    marginTop: 5
});

export const rightPanel = style({
    width: 300,
    float: "right",
    textAlign: "left"
});

export const rampsTable = style({
    borderCollapse: "collapse"
});

export const rampsTableHeader = style({
    backgroundColor: "#ffbf00",
    textAlign: "left",
    fontWeight: "bold",
    width: 150,
    color: "white",
    marginTop: 5
});

export const selectedProp = style({
    backgroundColor: "chartreuse",
});

export const unselectedProp = style({
    backgroundColor: "white",
});

export const boatMapx = style({
    width: 1000,
    height: 1000,
    float: "left"
});
